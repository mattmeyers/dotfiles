[core]
    editor = nvim

[init]
    defaultBranch = main

[alias]
    a = add
    c = commit
    s = status
    p = pull
    co = checkout
    us = restore --staged
    wtl = worktree list
    wta = worktree add
    wtr = worktree remove

[include]
    path = ~/.config/git/config.profile
